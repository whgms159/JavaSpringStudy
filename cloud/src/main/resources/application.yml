spring:
  application:
    name: cloud
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://18.216.217.159:3306/study_cloud
    username: root
    password: 1234
  jpa:
    database: mysql
    generate-ddl: true
    show-sql: true
    open-in-view: false
  sql:
    init:
      mode: never
      
  mvc:
    hiddenmethod:
      filter:
        enabled: true

server:
  port: 8080
     
