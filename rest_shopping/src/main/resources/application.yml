spring:
  application:
    name: rest_shopping
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shopping_db
    username: root
    password: 1234
  jpa:
    database: mysql
    generate-ddl: true
    show-sql: true
    open-in-view: false
  sql:
    init:
      mode: never
 